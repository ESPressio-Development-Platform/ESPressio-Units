// Generated by tools/generate_conversion_assets.py. Do not edit.
#include "ESPressio_Units.hpp"

#include <cmath>
#include <iostream>
#include <type_traits>
#include <utility>

using namespace ESPressio::Units;

template <typename...> struct MakeVoid { typedef void type; };
template <typename... T> using VoidT = typename MakeVoid<T...>::type;

template <typename TTarget, typename TEnable, typename... TInputs>
struct CanCreateFromImplementation : std::false_type { };

template <typename TTarget, typename... TInputs>
struct CanCreateFromImplementation<TTarget, VoidT<decltype(
    TTarget::From(std::declval<const TInputs&>()...)
)>, TInputs...> : std::true_type { };

template <typename TTarget, typename... TInputs>
struct CanCreateFrom : CanCreateFromImplementation<
    TTarget, void, TInputs...
> { };

int main() {
    unsigned checks = 0;
    static_assert(CanCreateFrom<Length<double>, Distance<double>>::value,
        "Registered Length formula must be callable");
    {
        const Length<double> result = Length<double>::From(Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Length<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Length factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Distance<double>, Length<double>>::value,
        "Registered Distance formula must be callable");
    {
        const Distance<double> result = Distance<double>::From(Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Distance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Distance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Speed<double>, Velocity<double>>::value,
        "Registered Speed formula must be callable");
    {
        const Speed<double> result = Speed<double>::From(Velocity<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Speed<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Speed factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Velocity<double>, Speed<double>>::value,
        "Registered Velocity formula must be callable");
    {
        const Velocity<double> result = Velocity<double>::From(Speed<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Velocity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Velocity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Force<double>, Weight<double>>::value,
        "Registered Force formula must be callable");
    {
        const Force<double> result = Force<double>::From(Weight<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Force<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Force factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Weight<double>, Force<double>>::value,
        "Registered Weight formula must be callable");
    {
        const Weight<double> result = Weight<double>::From(Force<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Weight<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Weight factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Pressure<double>, MechanicalStress<double>>::value,
        "Registered Pressure formula must be callable");
    {
        const Pressure<double> result = Pressure<double>::From(MechanicalStress<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Pressure<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Pressure factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MechanicalStress<double>, Pressure<double>>::value,
        "Registered MechanicalStress formula must be callable");
    {
        const MechanicalStress<double> result = MechanicalStress<double>::From(Pressure<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MechanicalStress<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MechanicalStress factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MomentOfForce<double>, Torque<double>>::value,
        "Registered MomentOfForce formula must be callable");
    {
        const MomentOfForce<double> result = MomentOfForce<double>::From(Torque<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MomentOfForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MomentOfForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Torque<double>, MomentOfForce<double>>::value,
        "Registered Torque formula must be callable");
    {
        const Torque<double> result = Torque<double>::From(MomentOfForce<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Torque<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Torque factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Density<double>, MassDensity<double>>::value,
        "Registered Density formula must be callable");
    {
        const Density<double> result = Density<double>::From(MassDensity<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Density<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Density factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MassDensity<double>, Density<double>>::value,
        "Registered MassDensity formula must be callable");
    {
        const MassDensity<double> result = MassDensity<double>::From(Density<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MassDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MassDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Energy<double>, Work<double>>::value,
        "Registered Energy formula must be callable");
    {
        const Energy<double> result = Energy<double>::From(Work<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Energy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Energy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Energy<double>, Heat<double>>::value,
        "Registered Energy formula must be callable");
    {
        const Energy<double> result = Energy<double>::From(Heat<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Energy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Energy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Work<double>, Energy<double>>::value,
        "Registered Work formula must be callable");
    {
        const Work<double> result = Work<double>::From(Energy<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Work<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Work factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Work<double>, Heat<double>>::value,
        "Registered Work formula must be callable");
    {
        const Work<double> result = Work<double>::From(Heat<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Work<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Work factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Heat<double>, Energy<double>>::value,
        "Registered Heat formula must be callable");
    {
        const Heat<double> result = Heat<double>::From(Energy<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Heat<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Heat factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Heat<double>, Work<double>>::value,
        "Registered Heat formula must be callable");
    {
        const Heat<double> result = Heat<double>::From(Work<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Heat<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Heat factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotential<double>, ElectricPotentialDifference<double>>::value,
        "Registered ElectricPotential formula must be callable");
    {
        const ElectricPotential<double> result = ElectricPotential<double>::From(ElectricPotentialDifference<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotential<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotential factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotential<double>, ElectromotiveForce<double>>::value,
        "Registered ElectricPotential formula must be callable");
    {
        const ElectricPotential<double> result = ElectricPotential<double>::From(ElectromotiveForce<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotential<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotential factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotentialDifference<double>, ElectricPotential<double>>::value,
        "Registered ElectricPotentialDifference formula must be callable");
    {
        const ElectricPotentialDifference<double> result = ElectricPotentialDifference<double>::From(ElectricPotential<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotentialDifference<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotentialDifference factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotentialDifference<double>, ElectromotiveForce<double>>::value,
        "Registered ElectricPotentialDifference formula must be callable");
    {
        const ElectricPotentialDifference<double> result = ElectricPotentialDifference<double>::From(ElectromotiveForce<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotentialDifference<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotentialDifference factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectromotiveForce<double>, ElectricPotential<double>>::value,
        "Registered ElectromotiveForce formula must be callable");
    {
        const ElectromotiveForce<double> result = ElectromotiveForce<double>::From(ElectricPotential<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectromotiveForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectromotiveForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectromotiveForce<double>, ElectricPotentialDifference<double>>::value,
        "Registered ElectromotiveForce formula must be callable");
    {
        const ElectromotiveForce<double> result = ElectromotiveForce<double>::From(ElectricPotentialDifference<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectromotiveForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectromotiveForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricFluxDensity<double>, ElectricDisplacement<double>>::value,
        "Registered ElectricFluxDensity formula must be callable");
    {
        const ElectricFluxDensity<double> result = ElectricFluxDensity<double>::From(ElectricDisplacement<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricFluxDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricFluxDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricDisplacement<double>, ElectricFluxDensity<double>>::value,
        "Registered ElectricDisplacement formula must be callable");
    {
        const ElectricDisplacement<double> result = ElectricDisplacement<double>::From(ElectricFluxDensity<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricDisplacement<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricDisplacement factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Frequency<double>, Time<double>>::value,
        "Registered Frequency formula must be callable");
    {
        const Frequency<double> result = Frequency<double>::From(Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Frequency<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Frequency factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Time<double>, Frequency<double>>::value,
        "Registered Time formula must be callable");
    {
        const Time<double> result = Time<double>::From(Frequency<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Time<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Time factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<AngularFrequency<double>, Frequency<double>>::value,
        "Registered AngularFrequency formula must be callable");
    {
        const AngularFrequency<double> result = AngularFrequency<double>::From(Frequency<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != AngularFrequency<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed AngularFrequency factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Frequency<double>, AngularFrequency<double>>::value,
        "Registered Frequency formula must be callable");
    {
        const Frequency<double> result = Frequency<double>::From(AngularFrequency<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Frequency<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Frequency factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ThermodynamicTemperature<double>, CelsiusTemperature<double>>::value,
        "Registered ThermodynamicTemperature formula must be callable");
    {
        const ThermodynamicTemperature<double> result = ThermodynamicTemperature<double>::From(CelsiusTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ThermodynamicTemperature<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ThermodynamicTemperature factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<CelsiusTemperature<double>, ThermodynamicTemperature<double>>::value,
        "Registered CelsiusTemperature formula must be callable");
    {
        const CelsiusTemperature<double> result = CelsiusTemperature<double>::From(ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != CelsiusTemperature<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed CelsiusTemperature factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricConductance<double>, ElectricResistance<double>>::value,
        "Registered ElectricConductance formula must be callable");
    {
        const ElectricConductance<double> result = ElectricConductance<double>::From(ElectricResistance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricConductance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricConductance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricResistance<double>, ElectricConductance<double>>::value,
        "Registered ElectricResistance formula must be callable");
    {
        const ElectricResistance<double> result = ElectricResistance<double>::From(ElectricConductance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricResistance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricResistance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Wavenumber<double>, Length<double>>::value,
        "Registered Wavenumber formula must be callable");
    {
        const Wavenumber<double> result = Wavenumber<double>::From(Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Wavenumber<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Wavenumber factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Wavenumber<double>, Distance<double>>::value,
        "Registered Wavenumber formula must be callable");
    {
        const Wavenumber<double> result = Wavenumber<double>::From(Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Wavenumber<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Wavenumber factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Area<double>, Length<double>, Length<double>>::value,
        "Registered Area formula must be callable");
    {
        const Area<double> result = Area<double>::From(Length<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Area<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Area factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Area<double>, Length<double>, Distance<double>>::value,
        "Registered Area formula must be callable");
    {
        const Area<double> result = Area<double>::From(Length<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Area<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Area factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Area<double>, Distance<double>, Length<double>>::value,
        "Registered Area formula must be callable");
    {
        const Area<double> result = Area<double>::From(Distance<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Area<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Area factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Area<double>, Distance<double>, Distance<double>>::value,
        "Registered Area formula must be callable");
    {
        const Area<double> result = Area<double>::From(Distance<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Area<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Area factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Volume<double>, Area<double>, Length<double>>::value,
        "Registered Volume formula must be callable");
    {
        const Volume<double> result = Volume<double>::From(Area<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Volume<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Volume factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Volume<double>, Area<double>, Distance<double>>::value,
        "Registered Volume formula must be callable");
    {
        const Volume<double> result = Volume<double>::From(Area<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Volume<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Volume factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Speed<double>, Length<double>, Time<double>>::value,
        "Registered Speed formula must be callable");
    {
        const Speed<double> result = Speed<double>::From(Length<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Speed<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Speed factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Speed<double>, Distance<double>, Time<double>>::value,
        "Registered Speed formula must be callable");
    {
        const Speed<double> result = Speed<double>::From(Distance<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Speed<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Speed factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Velocity<double>, Length<double>, Time<double>>::value,
        "Registered Velocity formula must be callable");
    {
        const Velocity<double> result = Velocity<double>::From(Length<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Velocity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Velocity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Velocity<double>, Distance<double>, Time<double>>::value,
        "Registered Velocity formula must be callable");
    {
        const Velocity<double> result = Velocity<double>::From(Distance<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Velocity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Velocity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Acceleration<double>, Speed<double>, Time<double>>::value,
        "Registered Acceleration formula must be callable");
    {
        const Acceleration<double> result = Acceleration<double>::From(Speed<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Acceleration<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Acceleration factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Acceleration<double>, Velocity<double>, Time<double>>::value,
        "Registered Acceleration formula must be callable");
    {
        const Acceleration<double> result = Acceleration<double>::From(Velocity<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Acceleration<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Acceleration factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<AngularVelocity<double>, PlaneAngle<double>, Time<double>>::value,
        "Registered AngularVelocity formula must be callable");
    {
        const AngularVelocity<double> result = AngularVelocity<double>::From(PlaneAngle<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != AngularVelocity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed AngularVelocity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<AngularAcceleration<double>, AngularVelocity<double>, Time<double>>::value,
        "Registered AngularAcceleration formula must be callable");
    {
        const AngularAcceleration<double> result = AngularAcceleration<double>::From(AngularVelocity<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != AngularAcceleration<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed AngularAcceleration factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Force<double>, Mass<double>, Acceleration<double>>::value,
        "Registered Force formula must be callable");
    {
        const Force<double> result = Force<double>::From(Mass<double>(2.0), Acceleration<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Force<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Force factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Weight<double>, Mass<double>, Acceleration<double>>::value,
        "Registered Weight formula must be callable");
    {
        const Weight<double> result = Weight<double>::From(Mass<double>(2.0), Acceleration<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Weight<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Weight factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Pressure<double>, Force<double>, Area<double>>::value,
        "Registered Pressure formula must be callable");
    {
        const Pressure<double> result = Pressure<double>::From(Force<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Pressure<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Pressure factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Pressure<double>, Weight<double>, Area<double>>::value,
        "Registered Pressure formula must be callable");
    {
        const Pressure<double> result = Pressure<double>::From(Weight<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Pressure<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Pressure factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MechanicalStress<double>, Force<double>, Area<double>>::value,
        "Registered MechanicalStress formula must be callable");
    {
        const MechanicalStress<double> result = MechanicalStress<double>::From(Force<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MechanicalStress<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MechanicalStress factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MechanicalStress<double>, Weight<double>, Area<double>>::value,
        "Registered MechanicalStress formula must be callable");
    {
        const MechanicalStress<double> result = MechanicalStress<double>::From(Weight<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MechanicalStress<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MechanicalStress factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MomentOfForce<double>, Force<double>, Length<double>>::value,
        "Registered MomentOfForce formula must be callable");
    {
        const MomentOfForce<double> result = MomentOfForce<double>::From(Force<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MomentOfForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MomentOfForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MomentOfForce<double>, Force<double>, Distance<double>>::value,
        "Registered MomentOfForce formula must be callable");
    {
        const MomentOfForce<double> result = MomentOfForce<double>::From(Force<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MomentOfForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MomentOfForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MomentOfForce<double>, Weight<double>, Length<double>>::value,
        "Registered MomentOfForce formula must be callable");
    {
        const MomentOfForce<double> result = MomentOfForce<double>::From(Weight<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MomentOfForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MomentOfForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MomentOfForce<double>, Weight<double>, Distance<double>>::value,
        "Registered MomentOfForce formula must be callable");
    {
        const MomentOfForce<double> result = MomentOfForce<double>::From(Weight<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MomentOfForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MomentOfForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Torque<double>, Force<double>, Length<double>>::value,
        "Registered Torque formula must be callable");
    {
        const Torque<double> result = Torque<double>::From(Force<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Torque<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Torque factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Torque<double>, Force<double>, Distance<double>>::value,
        "Registered Torque formula must be callable");
    {
        const Torque<double> result = Torque<double>::From(Force<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Torque<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Torque factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Torque<double>, Weight<double>, Length<double>>::value,
        "Registered Torque formula must be callable");
    {
        const Torque<double> result = Torque<double>::From(Weight<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Torque<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Torque factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Torque<double>, Weight<double>, Distance<double>>::value,
        "Registered Torque formula must be callable");
    {
        const Torque<double> result = Torque<double>::From(Weight<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Torque<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Torque factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SurfaceTension<double>, Force<double>, Length<double>>::value,
        "Registered SurfaceTension formula must be callable");
    {
        const SurfaceTension<double> result = SurfaceTension<double>::From(Force<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SurfaceTension<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SurfaceTension factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SurfaceTension<double>, Force<double>, Distance<double>>::value,
        "Registered SurfaceTension formula must be callable");
    {
        const SurfaceTension<double> result = SurfaceTension<double>::From(Force<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SurfaceTension<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SurfaceTension factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SurfaceTension<double>, Weight<double>, Length<double>>::value,
        "Registered SurfaceTension formula must be callable");
    {
        const SurfaceTension<double> result = SurfaceTension<double>::From(Weight<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SurfaceTension<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SurfaceTension factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SurfaceTension<double>, Weight<double>, Distance<double>>::value,
        "Registered SurfaceTension formula must be callable");
    {
        const SurfaceTension<double> result = SurfaceTension<double>::From(Weight<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SurfaceTension<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SurfaceTension factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Density<double>, Mass<double>, Volume<double>>::value,
        "Registered Density formula must be callable");
    {
        const Density<double> result = Density<double>::From(Mass<double>(2.0), Volume<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Density<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Density factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MassDensity<double>, Mass<double>, Volume<double>>::value,
        "Registered MassDensity formula must be callable");
    {
        const MassDensity<double> result = MassDensity<double>::From(Mass<double>(2.0), Volume<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MassDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MassDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SurfaceDensity<double>, Mass<double>, Area<double>>::value,
        "Registered SurfaceDensity formula must be callable");
    {
        const SurfaceDensity<double> result = SurfaceDensity<double>::From(Mass<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SurfaceDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SurfaceDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SpecificVolume<double>, Volume<double>, Mass<double>>::value,
        "Registered SpecificVolume formula must be callable");
    {
        const SpecificVolume<double> result = SpecificVolume<double>::From(Volume<double>(2.0), Mass<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SpecificVolume<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SpecificVolume factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<DynamicViscosity<double>, Pressure<double>, Time<double>>::value,
        "Registered DynamicViscosity formula must be callable");
    {
        const DynamicViscosity<double> result = DynamicViscosity<double>::From(Pressure<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != DynamicViscosity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed DynamicViscosity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<DynamicViscosity<double>, MechanicalStress<double>, Time<double>>::value,
        "Registered DynamicViscosity formula must be callable");
    {
        const DynamicViscosity<double> result = DynamicViscosity<double>::From(MechanicalStress<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != DynamicViscosity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed DynamicViscosity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<KinematicViscosity<double>, DynamicViscosity<double>, Density<double>>::value,
        "Registered KinematicViscosity formula must be callable");
    {
        const KinematicViscosity<double> result = KinematicViscosity<double>::From(DynamicViscosity<double>(2.0), Density<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != KinematicViscosity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed KinematicViscosity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<KinematicViscosity<double>, DynamicViscosity<double>, MassDensity<double>>::value,
        "Registered KinematicViscosity formula must be callable");
    {
        const KinematicViscosity<double> result = KinematicViscosity<double>::From(DynamicViscosity<double>(2.0), MassDensity<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != KinematicViscosity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed KinematicViscosity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Energy<double>, Force<double>, Length<double>>::value,
        "Registered Energy formula must be callable");
    {
        const Energy<double> result = Energy<double>::From(Force<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Energy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Energy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Energy<double>, Force<double>, Distance<double>>::value,
        "Registered Energy formula must be callable");
    {
        const Energy<double> result = Energy<double>::From(Force<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Energy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Energy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Energy<double>, Weight<double>, Length<double>>::value,
        "Registered Energy formula must be callable");
    {
        const Energy<double> result = Energy<double>::From(Weight<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Energy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Energy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Energy<double>, Weight<double>, Distance<double>>::value,
        "Registered Energy formula must be callable");
    {
        const Energy<double> result = Energy<double>::From(Weight<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Energy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Energy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Work<double>, Force<double>, Length<double>>::value,
        "Registered Work formula must be callable");
    {
        const Work<double> result = Work<double>::From(Force<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Work<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Work factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Work<double>, Force<double>, Distance<double>>::value,
        "Registered Work formula must be callable");
    {
        const Work<double> result = Work<double>::From(Force<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Work<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Work factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Work<double>, Weight<double>, Length<double>>::value,
        "Registered Work formula must be callable");
    {
        const Work<double> result = Work<double>::From(Weight<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Work<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Work factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Work<double>, Weight<double>, Distance<double>>::value,
        "Registered Work formula must be callable");
    {
        const Work<double> result = Work<double>::From(Weight<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Work<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Work factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Heat<double>, Force<double>, Length<double>>::value,
        "Registered Heat formula must be callable");
    {
        const Heat<double> result = Heat<double>::From(Force<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Heat<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Heat factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Heat<double>, Force<double>, Distance<double>>::value,
        "Registered Heat formula must be callable");
    {
        const Heat<double> result = Heat<double>::From(Force<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Heat<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Heat factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Heat<double>, Weight<double>, Length<double>>::value,
        "Registered Heat formula must be callable");
    {
        const Heat<double> result = Heat<double>::From(Weight<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Heat<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Heat factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Heat<double>, Weight<double>, Distance<double>>::value,
        "Registered Heat formula must be callable");
    {
        const Heat<double> result = Heat<double>::From(Weight<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Heat<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Heat factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Energy<double>, Power<double>, Time<double>>::value,
        "Registered Energy formula must be callable");
    {
        const Energy<double> result = Energy<double>::From(Power<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Energy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Energy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Work<double>, Power<double>, Time<double>>::value,
        "Registered Work formula must be callable");
    {
        const Work<double> result = Work<double>::From(Power<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Work<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Work factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Heat<double>, Power<double>, Time<double>>::value,
        "Registered Heat formula must be callable");
    {
        const Heat<double> result = Heat<double>::From(Power<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Heat<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Heat factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Power<double>, Energy<double>, Time<double>>::value,
        "Registered Power formula must be callable");
    {
        const Power<double> result = Power<double>::From(Energy<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Power<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Power factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Power<double>, Work<double>, Time<double>>::value,
        "Registered Power formula must be callable");
    {
        const Power<double> result = Power<double>::From(Work<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Power<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Power factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Power<double>, Heat<double>, Time<double>>::value,
        "Registered Power formula must be callable");
    {
        const Power<double> result = Power<double>::From(Heat<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Power<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Power factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Power<double>, Force<double>, Speed<double>>::value,
        "Registered Power formula must be callable");
    {
        const Power<double> result = Power<double>::From(Force<double>(2.0), Speed<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Power<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Power factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Power<double>, Force<double>, Velocity<double>>::value,
        "Registered Power formula must be callable");
    {
        const Power<double> result = Power<double>::From(Force<double>(2.0), Velocity<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Power<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Power factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Power<double>, Weight<double>, Speed<double>>::value,
        "Registered Power formula must be callable");
    {
        const Power<double> result = Power<double>::From(Weight<double>(2.0), Speed<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Power<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Power factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Power<double>, Weight<double>, Velocity<double>>::value,
        "Registered Power formula must be callable");
    {
        const Power<double> result = Power<double>::From(Weight<double>(2.0), Velocity<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Power<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Power factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SpecificEnergy<double>, Energy<double>, Mass<double>>::value,
        "Registered SpecificEnergy formula must be callable");
    {
        const SpecificEnergy<double> result = SpecificEnergy<double>::From(Energy<double>(2.0), Mass<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SpecificEnergy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SpecificEnergy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SpecificEnergy<double>, Work<double>, Mass<double>>::value,
        "Registered SpecificEnergy formula must be callable");
    {
        const SpecificEnergy<double> result = SpecificEnergy<double>::From(Work<double>(2.0), Mass<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SpecificEnergy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SpecificEnergy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SpecificEnergy<double>, Heat<double>, Mass<double>>::value,
        "Registered SpecificEnergy formula must be callable");
    {
        const SpecificEnergy<double> result = SpecificEnergy<double>::From(Heat<double>(2.0), Mass<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SpecificEnergy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SpecificEnergy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<EnergyDensity<double>, Energy<double>, Volume<double>>::value,
        "Registered EnergyDensity formula must be callable");
    {
        const EnergyDensity<double> result = EnergyDensity<double>::From(Energy<double>(2.0), Volume<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != EnergyDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed EnergyDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<EnergyDensity<double>, Work<double>, Volume<double>>::value,
        "Registered EnergyDensity formula must be callable");
    {
        const EnergyDensity<double> result = EnergyDensity<double>::From(Work<double>(2.0), Volume<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != EnergyDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed EnergyDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<EnergyDensity<double>, Heat<double>, Volume<double>>::value,
        "Registered EnergyDensity formula must be callable");
    {
        const EnergyDensity<double> result = EnergyDensity<double>::From(Heat<double>(2.0), Volume<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != EnergyDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed EnergyDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<HeatCapacity<double>, Energy<double>, ThermodynamicTemperature<double>>::value,
        "Registered HeatCapacity formula must be callable");
    {
        const HeatCapacity<double> result = HeatCapacity<double>::From(Energy<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != HeatCapacity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed HeatCapacity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<HeatCapacity<double>, Heat<double>, ThermodynamicTemperature<double>>::value,
        "Registered HeatCapacity formula must be callable");
    {
        const HeatCapacity<double> result = HeatCapacity<double>::From(Heat<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != HeatCapacity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed HeatCapacity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Entropy<double>, Energy<double>, ThermodynamicTemperature<double>>::value,
        "Registered Entropy formula must be callable");
    {
        const Entropy<double> result = Entropy<double>::From(Energy<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Entropy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Entropy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Entropy<double>, Heat<double>, ThermodynamicTemperature<double>>::value,
        "Registered Entropy formula must be callable");
    {
        const Entropy<double> result = Entropy<double>::From(Heat<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Entropy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Entropy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<HeatFluxDensity<double>, Power<double>, Area<double>>::value,
        "Registered HeatFluxDensity formula must be callable");
    {
        const HeatFluxDensity<double> result = HeatFluxDensity<double>::From(Power<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != HeatFluxDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed HeatFluxDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Irradiance<double>, Power<double>, Area<double>>::value,
        "Registered Irradiance formula must be callable");
    {
        const Irradiance<double> result = Irradiance<double>::From(Power<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Irradiance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Irradiance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricCharge<double>, ElectricCurrent<double>, Time<double>>::value,
        "Registered ElectricCharge formula must be callable");
    {
        const ElectricCharge<double> result = ElectricCharge<double>::From(ElectricCurrent<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricCharge<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricCharge factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotential<double>, Energy<double>, ElectricCharge<double>>::value,
        "Registered ElectricPotential formula must be callable");
    {
        const ElectricPotential<double> result = ElectricPotential<double>::From(Energy<double>(2.0), ElectricCharge<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotential<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotential factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotential<double>, Work<double>, ElectricCharge<double>>::value,
        "Registered ElectricPotential formula must be callable");
    {
        const ElectricPotential<double> result = ElectricPotential<double>::From(Work<double>(2.0), ElectricCharge<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotential<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotential factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotentialDifference<double>, Energy<double>, ElectricCharge<double>>::value,
        "Registered ElectricPotentialDifference formula must be callable");
    {
        const ElectricPotentialDifference<double> result = ElectricPotentialDifference<double>::From(Energy<double>(2.0), ElectricCharge<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotentialDifference<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotentialDifference factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotentialDifference<double>, Work<double>, ElectricCharge<double>>::value,
        "Registered ElectricPotentialDifference formula must be callable");
    {
        const ElectricPotentialDifference<double> result = ElectricPotentialDifference<double>::From(Work<double>(2.0), ElectricCharge<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotentialDifference<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotentialDifference factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectromotiveForce<double>, Energy<double>, ElectricCharge<double>>::value,
        "Registered ElectromotiveForce formula must be callable");
    {
        const ElectromotiveForce<double> result = ElectromotiveForce<double>::From(Energy<double>(2.0), ElectricCharge<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectromotiveForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectromotiveForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectromotiveForce<double>, Work<double>, ElectricCharge<double>>::value,
        "Registered ElectromotiveForce formula must be callable");
    {
        const ElectromotiveForce<double> result = ElectromotiveForce<double>::From(Work<double>(2.0), ElectricCharge<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectromotiveForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectromotiveForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotential<double>, Power<double>, ElectricCurrent<double>>::value,
        "Registered ElectricPotential formula must be callable");
    {
        const ElectricPotential<double> result = ElectricPotential<double>::From(Power<double>(2.0), ElectricCurrent<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotential<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotential factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricPotentialDifference<double>, Power<double>, ElectricCurrent<double>>::value,
        "Registered ElectricPotentialDifference formula must be callable");
    {
        const ElectricPotentialDifference<double> result = ElectricPotentialDifference<double>::From(Power<double>(2.0), ElectricCurrent<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricPotentialDifference<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotentialDifference factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectromotiveForce<double>, Power<double>, ElectricCurrent<double>>::value,
        "Registered ElectromotiveForce formula must be callable");
    {
        const ElectromotiveForce<double> result = ElectromotiveForce<double>::From(Power<double>(2.0), ElectricCurrent<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectromotiveForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectromotiveForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Capacitance<double>, ElectricCharge<double>, ElectricPotential<double>>::value,
        "Registered Capacitance formula must be callable");
    {
        const Capacitance<double> result = Capacitance<double>::From(ElectricCharge<double>(2.0), ElectricPotential<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Capacitance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Capacitance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Capacitance<double>, ElectricCharge<double>, ElectricPotentialDifference<double>>::value,
        "Registered Capacitance formula must be callable");
    {
        const Capacitance<double> result = Capacitance<double>::From(ElectricCharge<double>(2.0), ElectricPotentialDifference<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Capacitance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Capacitance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Capacitance<double>, ElectricCharge<double>, ElectromotiveForce<double>>::value,
        "Registered Capacitance formula must be callable");
    {
        const Capacitance<double> result = Capacitance<double>::From(ElectricCharge<double>(2.0), ElectromotiveForce<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Capacitance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Capacitance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricResistance<double>, ElectricPotential<double>, ElectricCurrent<double>>::value,
        "Registered ElectricResistance formula must be callable");
    {
        const ElectricResistance<double> result = ElectricResistance<double>::From(ElectricPotential<double>(2.0), ElectricCurrent<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricResistance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricResistance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricResistance<double>, ElectricPotentialDifference<double>, ElectricCurrent<double>>::value,
        "Registered ElectricResistance formula must be callable");
    {
        const ElectricResistance<double> result = ElectricResistance<double>::From(ElectricPotentialDifference<double>(2.0), ElectricCurrent<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricResistance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricResistance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricResistance<double>, ElectromotiveForce<double>, ElectricCurrent<double>>::value,
        "Registered ElectricResistance formula must be callable");
    {
        const ElectricResistance<double> result = ElectricResistance<double>::From(ElectromotiveForce<double>(2.0), ElectricCurrent<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricResistance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricResistance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricConductance<double>, ElectricCurrent<double>, ElectricPotential<double>>::value,
        "Registered ElectricConductance formula must be callable");
    {
        const ElectricConductance<double> result = ElectricConductance<double>::From(ElectricCurrent<double>(2.0), ElectricPotential<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricConductance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricConductance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricConductance<double>, ElectricCurrent<double>, ElectricPotentialDifference<double>>::value,
        "Registered ElectricConductance formula must be callable");
    {
        const ElectricConductance<double> result = ElectricConductance<double>::From(ElectricCurrent<double>(2.0), ElectricPotentialDifference<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricConductance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricConductance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricConductance<double>, ElectricCurrent<double>, ElectromotiveForce<double>>::value,
        "Registered ElectricConductance formula must be callable");
    {
        const ElectricConductance<double> result = ElectricConductance<double>::From(ElectricCurrent<double>(2.0), ElectromotiveForce<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricConductance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricConductance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<CurrentDensity<double>, ElectricCurrent<double>, Area<double>>::value,
        "Registered CurrentDensity formula must be callable");
    {
        const CurrentDensity<double> result = CurrentDensity<double>::From(ElectricCurrent<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != CurrentDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed CurrentDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricFieldStrength<double>, ElectricPotential<double>, Length<double>>::value,
        "Registered ElectricFieldStrength formula must be callable");
    {
        const ElectricFieldStrength<double> result = ElectricFieldStrength<double>::From(ElectricPotential<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricFieldStrength<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricFieldStrength factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricFieldStrength<double>, ElectricPotential<double>, Distance<double>>::value,
        "Registered ElectricFieldStrength formula must be callable");
    {
        const ElectricFieldStrength<double> result = ElectricFieldStrength<double>::From(ElectricPotential<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricFieldStrength<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricFieldStrength factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricFieldStrength<double>, ElectricPotentialDifference<double>, Length<double>>::value,
        "Registered ElectricFieldStrength formula must be callable");
    {
        const ElectricFieldStrength<double> result = ElectricFieldStrength<double>::From(ElectricPotentialDifference<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricFieldStrength<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricFieldStrength factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricFieldStrength<double>, ElectricPotentialDifference<double>, Distance<double>>::value,
        "Registered ElectricFieldStrength formula must be callable");
    {
        const ElectricFieldStrength<double> result = ElectricFieldStrength<double>::From(ElectricPotentialDifference<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricFieldStrength<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricFieldStrength factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricFieldStrength<double>, ElectromotiveForce<double>, Length<double>>::value,
        "Registered ElectricFieldStrength formula must be callable");
    {
        const ElectricFieldStrength<double> result = ElectricFieldStrength<double>::From(ElectromotiveForce<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricFieldStrength<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricFieldStrength factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricFieldStrength<double>, ElectromotiveForce<double>, Distance<double>>::value,
        "Registered ElectricFieldStrength formula must be callable");
    {
        const ElectricFieldStrength<double> result = ElectricFieldStrength<double>::From(ElectromotiveForce<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricFieldStrength<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricFieldStrength factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricChargeDensity<double>, ElectricCharge<double>, Volume<double>>::value,
        "Registered ElectricChargeDensity formula must be callable");
    {
        const ElectricChargeDensity<double> result = ElectricChargeDensity<double>::From(ElectricCharge<double>(2.0), Volume<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricChargeDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricChargeDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SurfaceChargeDensity<double>, ElectricCharge<double>, Area<double>>::value,
        "Registered SurfaceChargeDensity formula must be callable");
    {
        const SurfaceChargeDensity<double> result = SurfaceChargeDensity<double>::From(ElectricCharge<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SurfaceChargeDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SurfaceChargeDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricFluxDensity<double>, ElectricCharge<double>, Area<double>>::value,
        "Registered ElectricFluxDensity formula must be callable");
    {
        const ElectricFluxDensity<double> result = ElectricFluxDensity<double>::From(ElectricCharge<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricFluxDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricFluxDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ElectricDisplacement<double>, ElectricCharge<double>, Area<double>>::value,
        "Registered ElectricDisplacement formula must be callable");
    {
        const ElectricDisplacement<double> result = ElectricDisplacement<double>::From(ElectricCharge<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ElectricDisplacement<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricDisplacement factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Permittivity<double>, Capacitance<double>, Length<double>>::value,
        "Registered Permittivity formula must be callable");
    {
        const Permittivity<double> result = Permittivity<double>::From(Capacitance<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Permittivity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Permittivity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Permittivity<double>, Capacitance<double>, Distance<double>>::value,
        "Registered Permittivity formula must be callable");
    {
        const Permittivity<double> result = Permittivity<double>::From(Capacitance<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Permittivity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Permittivity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MagneticFieldStrength<double>, ElectricCurrent<double>, Length<double>>::value,
        "Registered MagneticFieldStrength formula must be callable");
    {
        const MagneticFieldStrength<double> result = MagneticFieldStrength<double>::From(ElectricCurrent<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MagneticFieldStrength<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MagneticFieldStrength factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MagneticFieldStrength<double>, ElectricCurrent<double>, Distance<double>>::value,
        "Registered MagneticFieldStrength formula must be callable");
    {
        const MagneticFieldStrength<double> result = MagneticFieldStrength<double>::From(ElectricCurrent<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MagneticFieldStrength<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MagneticFieldStrength factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MagneticFlux<double>, ElectricPotential<double>, Time<double>>::value,
        "Registered MagneticFlux formula must be callable");
    {
        const MagneticFlux<double> result = MagneticFlux<double>::From(ElectricPotential<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MagneticFlux<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MagneticFlux factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MagneticFlux<double>, ElectricPotentialDifference<double>, Time<double>>::value,
        "Registered MagneticFlux formula must be callable");
    {
        const MagneticFlux<double> result = MagneticFlux<double>::From(ElectricPotentialDifference<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MagneticFlux<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MagneticFlux factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MagneticFlux<double>, ElectromotiveForce<double>, Time<double>>::value,
        "Registered MagneticFlux formula must be callable");
    {
        const MagneticFlux<double> result = MagneticFlux<double>::From(ElectromotiveForce<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MagneticFlux<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MagneticFlux factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MagneticFluxDensity<double>, MagneticFlux<double>, Area<double>>::value,
        "Registered MagneticFluxDensity formula must be callable");
    {
        const MagneticFluxDensity<double> result = MagneticFluxDensity<double>::From(MagneticFlux<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MagneticFluxDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MagneticFluxDensity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MagnetomotiveForce<double>, ElectricCurrent<double>, Ratio<double>>::value,
        "Registered MagnetomotiveForce formula must be callable");
    {
        const MagnetomotiveForce<double> result = MagnetomotiveForce<double>::From(ElectricCurrent<double>(2.0), Ratio<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MagnetomotiveForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MagnetomotiveForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MagnetomotiveForce<double>, ElectricCurrent<double>, Dimensionless<double>>::value,
        "Registered MagnetomotiveForce formula must be callable");
    {
        const MagnetomotiveForce<double> result = MagnetomotiveForce<double>::From(ElectricCurrent<double>(2.0), Dimensionless<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MagnetomotiveForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MagnetomotiveForce factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Inductance<double>, MagneticFlux<double>, ElectricCurrent<double>>::value,
        "Registered Inductance formula must be callable");
    {
        const Inductance<double> result = Inductance<double>::From(MagneticFlux<double>(2.0), ElectricCurrent<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Inductance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Inductance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Permeability<double>, Inductance<double>, Length<double>>::value,
        "Registered Permeability formula must be callable");
    {
        const Permeability<double> result = Permeability<double>::From(Inductance<double>(2.0), Length<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Permeability<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Permeability factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Permeability<double>, Inductance<double>, Distance<double>>::value,
        "Registered Permeability formula must be callable");
    {
        const Permeability<double> result = Permeability<double>::From(Inductance<double>(2.0), Distance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Permeability<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Permeability factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<LuminousFlux<double>, LuminousIntensity<double>, SolidAngle<double>>::value,
        "Registered LuminousFlux formula must be callable");
    {
        const LuminousFlux<double> result = LuminousFlux<double>::From(LuminousIntensity<double>(2.0), SolidAngle<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != LuminousFlux<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed LuminousFlux factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Illuminance<double>, LuminousFlux<double>, Area<double>>::value,
        "Registered Illuminance formula must be callable");
    {
        const Illuminance<double> result = Illuminance<double>::From(LuminousFlux<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Illuminance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Illuminance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Luminance<double>, LuminousIntensity<double>, Area<double>>::value,
        "Registered Luminance formula must be callable");
    {
        const Luminance<double> result = Luminance<double>::From(LuminousIntensity<double>(2.0), Area<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Luminance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Luminance factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<AmountOfSubstanceConcentration<double>, AmountOfSubstance<double>, Volume<double>>::value,
        "Registered AmountOfSubstanceConcentration formula must be callable");
    {
        const AmountOfSubstanceConcentration<double> result = AmountOfSubstanceConcentration<double>::From(AmountOfSubstance<double>(2.0), Volume<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != AmountOfSubstanceConcentration<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed AmountOfSubstanceConcentration factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MassConcentration<double>, Mass<double>, Volume<double>>::value,
        "Registered MassConcentration formula must be callable");
    {
        const MassConcentration<double> result = MassConcentration<double>::From(Mass<double>(2.0), Volume<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MassConcentration<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MassConcentration factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MolarEnergy<double>, Energy<double>, AmountOfSubstance<double>>::value,
        "Registered MolarEnergy formula must be callable");
    {
        const MolarEnergy<double> result = MolarEnergy<double>::From(Energy<double>(2.0), AmountOfSubstance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MolarEnergy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MolarEnergy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MolarEnergy<double>, Work<double>, AmountOfSubstance<double>>::value,
        "Registered MolarEnergy formula must be callable");
    {
        const MolarEnergy<double> result = MolarEnergy<double>::From(Work<double>(2.0), AmountOfSubstance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MolarEnergy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MolarEnergy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MolarEnergy<double>, Heat<double>, AmountOfSubstance<double>>::value,
        "Registered MolarEnergy formula must be callable");
    {
        const MolarEnergy<double> result = MolarEnergy<double>::From(Heat<double>(2.0), AmountOfSubstance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MolarEnergy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MolarEnergy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<MolarEntropy<double>, Entropy<double>, AmountOfSubstance<double>>::value,
        "Registered MolarEntropy formula must be callable");
    {
        const MolarEntropy<double> result = MolarEntropy<double>::From(Entropy<double>(2.0), AmountOfSubstance<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != MolarEntropy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MolarEntropy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<CatalyticActivity<double>, AmountOfSubstance<double>, Time<double>>::value,
        "Registered CatalyticActivity formula must be callable");
    {
        const CatalyticActivity<double> result = CatalyticActivity<double>::From(AmountOfSubstance<double>(2.0), Time<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != CatalyticActivity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed CatalyticActivity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<AbsorbedDose<double>, Energy<double>, Mass<double>>::value,
        "Registered AbsorbedDose formula must be callable");
    {
        const AbsorbedDose<double> result = AbsorbedDose<double>::From(Energy<double>(2.0), Mass<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != AbsorbedDose<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed AbsorbedDose factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Kerma<double>, Energy<double>, Mass<double>>::value,
        "Registered Kerma formula must be callable");
    {
        const Kerma<double> result = Kerma<double>::From(Energy<double>(2.0), Mass<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Kerma<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Kerma factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<DoseEquivalent<double>, AbsorbedDose<double>, Ratio<double>>::value,
        "Registered DoseEquivalent formula must be callable");
    {
        const DoseEquivalent<double> result = DoseEquivalent<double>::From(AbsorbedDose<double>(2.0), Ratio<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != DoseEquivalent<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed DoseEquivalent factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<DoseEquivalent<double>, AbsorbedDose<double>, Dimensionless<double>>::value,
        "Registered DoseEquivalent formula must be callable");
    {
        const DoseEquivalent<double> result = DoseEquivalent<double>::From(AbsorbedDose<double>(2.0), Dimensionless<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != DoseEquivalent<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed DoseEquivalent factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<DoseEquivalent<double>, Kerma<double>, Ratio<double>>::value,
        "Registered DoseEquivalent formula must be callable");
    {
        const DoseEquivalent<double> result = DoseEquivalent<double>::From(Kerma<double>(2.0), Ratio<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != DoseEquivalent<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed DoseEquivalent factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<DoseEquivalent<double>, Kerma<double>, Dimensionless<double>>::value,
        "Registered DoseEquivalent formula must be callable");
    {
        const DoseEquivalent<double> result = DoseEquivalent<double>::From(Kerma<double>(2.0), Dimensionless<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != DoseEquivalent<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed DoseEquivalent factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<Exposure<double>, ElectricCharge<double>, Mass<double>>::value,
        "Registered Exposure formula must be callable");
    {
        const Exposure<double> result = Exposure<double>::From(ElectricCharge<double>(2.0), Mass<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != Exposure<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Exposure factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SpecificHeatCapacity<double>, Energy<double>, Mass<double>, ThermodynamicTemperature<double>>::value,
        "Registered SpecificHeatCapacity formula must be callable");
    {
        const SpecificHeatCapacity<double> result = SpecificHeatCapacity<double>::From(Energy<double>(2.0), Mass<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SpecificHeatCapacity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SpecificHeatCapacity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SpecificHeatCapacity<double>, Heat<double>, Mass<double>, ThermodynamicTemperature<double>>::value,
        "Registered SpecificHeatCapacity formula must be callable");
    {
        const SpecificHeatCapacity<double> result = SpecificHeatCapacity<double>::From(Heat<double>(2.0), Mass<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SpecificHeatCapacity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SpecificHeatCapacity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SpecificEntropy<double>, Energy<double>, Mass<double>, ThermodynamicTemperature<double>>::value,
        "Registered SpecificEntropy formula must be callable");
    {
        const SpecificEntropy<double> result = SpecificEntropy<double>::From(Energy<double>(2.0), Mass<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SpecificEntropy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SpecificEntropy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<SpecificEntropy<double>, Heat<double>, Mass<double>, ThermodynamicTemperature<double>>::value,
        "Registered SpecificEntropy formula must be callable");
    {
        const SpecificEntropy<double> result = SpecificEntropy<double>::From(Heat<double>(2.0), Mass<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != SpecificEntropy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed SpecificEntropy factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ThermalConductivity<double>, Power<double>, Length<double>, ThermodynamicTemperature<double>>::value,
        "Registered ThermalConductivity formula must be callable");
    {
        const ThermalConductivity<double> result = ThermalConductivity<double>::From(Power<double>(2.0), Length<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ThermalConductivity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ThermalConductivity factory contract\n";
            return 1;
        }
        ++checks;
    }
    static_assert(CanCreateFrom<ThermalConductivity<double>, Power<double>, Distance<double>, ThermodynamicTemperature<double>>::value,
        "Registered ThermalConductivity formula must be callable");
    {
        const ThermalConductivity<double> result = ThermalConductivity<double>::From(Power<double>(2.0), Distance<double>(2.0), ThermodynamicTemperature<double>(2.0));
        if (!std::isfinite(result.value) ||
            result.orderOfMagnitude != ThermalConductivity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ThermalConductivity factory contract\n";
            return 1;
        }
        ++checks;
    }
    {
        const Distance<double> result = Length<double>(2.0).ToContext<Distance, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Distance<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Length to Distance direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Length<double> result = Distance<double>(2.0).ToContext<Length, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Length<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Distance to Length direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Velocity<double> result = Speed<double>(2.0).ToContext<Velocity, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Velocity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Speed to Velocity direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Speed<double> result = Velocity<double>(2.0).ToContext<Speed, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Speed<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Velocity to Speed direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Weight<double> result = Force<double>(2.0).ToContext<Weight, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Weight<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Force to Weight direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Force<double> result = Weight<double>(2.0).ToContext<Force, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Force<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Weight to Force direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const MechanicalStress<double> result = Pressure<double>(2.0).ToContext<MechanicalStress, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != MechanicalStress<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Pressure to MechanicalStress direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Pressure<double> result = MechanicalStress<double>(2.0).ToContext<Pressure, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Pressure<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MechanicalStress to Pressure direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Torque<double> result = MomentOfForce<double>(2.0).ToContext<Torque, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Torque<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MomentOfForce to Torque direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const MomentOfForce<double> result = Torque<double>(2.0).ToContext<MomentOfForce, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != MomentOfForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Torque to MomentOfForce direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const MassDensity<double> result = Density<double>(2.0).ToContext<MassDensity, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != MassDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Density to MassDensity direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Density<double> result = MassDensity<double>(2.0).ToContext<Density, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Density<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed MassDensity to Density direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Work<double> result = Energy<double>(2.0).ToContext<Work, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Work<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Energy to Work direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Heat<double> result = Energy<double>(2.0).ToContext<Heat, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Heat<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Energy to Heat direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Energy<double> result = Work<double>(2.0).ToContext<Energy, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Energy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Work to Energy direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Heat<double> result = Work<double>(2.0).ToContext<Heat, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Heat<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Work to Heat direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Energy<double> result = Heat<double>(2.0).ToContext<Energy, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Energy<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Heat to Energy direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const Work<double> result = Heat<double>(2.0).ToContext<Work, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != Work<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed Heat to Work direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const ElectricPotentialDifference<double> result = ElectricPotential<double>(2.0).ToContext<ElectricPotentialDifference, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != ElectricPotentialDifference<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotential to ElectricPotentialDifference direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const ElectromotiveForce<double> result = ElectricPotential<double>(2.0).ToContext<ElectromotiveForce, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != ElectromotiveForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotential to ElectromotiveForce direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const ElectricPotential<double> result = ElectricPotentialDifference<double>(2.0).ToContext<ElectricPotential, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != ElectricPotential<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotentialDifference to ElectricPotential direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const ElectromotiveForce<double> result = ElectricPotentialDifference<double>(2.0).ToContext<ElectromotiveForce, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != ElectromotiveForce<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricPotentialDifference to ElectromotiveForce direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const ElectricPotential<double> result = ElectromotiveForce<double>(2.0).ToContext<ElectricPotential, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != ElectricPotential<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectromotiveForce to ElectricPotential direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const ElectricPotentialDifference<double> result = ElectromotiveForce<double>(2.0).ToContext<ElectricPotentialDifference, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != ElectricPotentialDifference<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectromotiveForce to ElectricPotentialDifference direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const ElectricDisplacement<double> result = ElectricFluxDensity<double>(2.0).ToContext<ElectricDisplacement, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != ElectricDisplacement<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricFluxDensity to ElectricDisplacement direct conversion\n";
            return 1;
        }
        ++checks;
    }
    {
        const ElectricFluxDensity<double> result = ElectricDisplacement<double>(2.0).ToContext<ElectricFluxDensity, double>();
        if (result.value != 2.0 ||
            result.orderOfMagnitude != ElectricFluxDensity<double>::baseOrderOfMagnitude) {
            std::cerr << "Failed ElectricDisplacement to ElectricFluxDensity direct conversion\n";
            return 1;
        }
        ++checks;
    }
    static_assert(!CanCreateFrom<Velocity<double>, double, Time<double>>::value,
        "Raw numeric inputs must be rejected");
    static_assert(!CanCreateFrom<Velocity<double>, Mass<double>, Time<double>>::value,
        "Dimensionally invalid unit inputs must be rejected");

    const Velocity<double> normalized = Velocity<double>::From(
        Distance<double>(100.0, Base),
        Time<double>(500.0, Milli)
    );
    if (std::fabs(normalized.value - 200.0) > 1.0e-12) {
        std::cerr << "Magnitude normalization failed\n";
        return 1;
    }
    ++checks;

    const Velocity<double> predefinedMagnitudeInput =
        Velocity<double>::From(
            Distance<double>(100.0), MilliSeconds<double>(500.0)
        );
    if (std::fabs(predefinedMagnitudeInput.value - 200.0) > 1.0e-12) {
        std::cerr << "Predefined time input normalization failed\n";
        return 1;
    }
    ++checks;

    const MilliSeconds<double> predefinedMagnitudeResult =
        MilliSeconds<double>::From(Frequency<double>(2.0));
    if (std::fabs(predefinedMagnitudeResult.value - 500.0) > 1.0e-12 ||
        predefinedMagnitudeResult.orderOfMagnitude != Milli) {
        std::cerr << "Predefined time result scaling failed\n";
        return 1;
    }
    ++checks;

    const Velocity<int> rounded = Velocity<int>::From(
        Distance<double>(5.0), Time<double>(2.0)
    );
    if (rounded.value != 3) {
        std::cerr << "Integral formula result was not rounded\n";
        return 1;
    }
    ++checks;

    bool overflowRejected = false;
    try {
        (void) Velocity<signed char>::From(
            Distance<double>(1000.0), Time<double>(1.0)
        );
    } catch (const std::overflow_error&) {
        overflowRejected = true;
    }
    if (!overflowRejected) {
        std::cerr << "Out-of-range formula result was not rejected\n";
        return 1;
    }
    ++checks;

    const int uncheckedMagnitude =
        Distance<int>(1, Mega).ToMagnitudeUnchecked<int>(Kilo);
    if (uncheckedMagnitude != 1000) {
        std::cerr << "Unchecked magnitude conversion failed\n";
        return 1;
    }
    ++checks;

    const Velocity<int> uncheckedFormula =
        Velocity<int>::FromUnchecked(
            Distance<double>(5.0), Time<double>(2.0)
        );
    if (uncheckedFormula.value != 3) {
        std::cerr << "Unchecked formula conversion failed\n";
        return 1;
    }
    ++checks;

    bool divisionByZeroRejected = false;
    try {
        (void) Velocity<double>::From(Distance<double>(1.0), Time<double>(0.0));
    } catch (const std::domain_error&) {
        divisionByZeroRejected = true;
    }
    if (!divisionByZeroRejected) {
        std::cerr << "Zero denominator was not rejected\n";
        return 1;
    }
    ++checks;

    std::cout << "Passed " << checks
              << " generated conversion checks across "
              << 176 << " formulas and "
              << 26 << " direct conversions.\n";
    return 0;
}
